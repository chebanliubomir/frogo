name: CI for project "FROGO"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [main]

jobs:
  installation:
    runs-on: ubuntu-latest

    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: "qwqzx1485@gmail.com"
          password: "2webdM9cCmmt6UV#kCF@vB"

      - name: Set up Docker
        uses: docker/setup-buildx-action@v3

      - name: Run to Docker
        run: docker compose up
